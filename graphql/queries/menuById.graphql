query GetMenuItemById($id: uuid!) {
  menu_items_by_pk(id: $id) {
    id
    category_id
    name
    description
    ingredients
    price
    calories
    allergens
    image_url
    is_available
    created_at
    updated_at
  }
}
